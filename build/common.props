<Project>
    <Import Project="version.props" />
    <PropertyGroup>
        <PackageLicenseExpression>MIT</PackageLicenseExpression>
        <Authors>pauldotknopf</Authors>
        <PackageDescription>A tool to build apt repos from source inputs.</PackageDescription>
        <Summary>A tool to build apt repos from source inputs.</Summary>
        <RepositoryUrl>https://github.com/pauldotknopf/apt-repo-builder</RepositoryUrl>
        <PackageProjectUrl>https://github.com/pauldotknopf/apt-repo-builder</PackageProjectUrl>
    </PropertyGroup>
</Project>